module.exports = class botDB {

    constructor(db) {
        this.db = db
    }

    insert(key, building) {

    }

    checkBot(key) {

        // return building if key valid, not permited otherwise
    }

}